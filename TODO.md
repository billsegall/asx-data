# TODO

- Stock splits: detect and handle capital consolidation / reverse split events so
  that the 52-week high/low (and chart history) are adjusted correctly. BTR is an
  example where the raw data shows a ~30x move that may partly reflect a split.
