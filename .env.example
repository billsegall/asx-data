# asx-data environment variables
# Copy to .env and fill in values, or export before running.

DATABASE=../stockdb/stockdb.db
FRONTEND_URL=http://localhost:5000
ANALYSIS_RESULTS_DIR=../analysis/results

# For analysis/sync.sh — SSH target for DB sync
ASX_SERVER=user@your-server
