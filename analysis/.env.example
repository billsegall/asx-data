# Environment variables for analysis/sync.sh
# Copy to ../.env and fill in your values (never commit .env)

# Remote server where stockdb.db lives and results are pushed to
ASX_SERVER=user@your-server

# GPU machine (used if orchestrating sync from a remote host)
REMOTE_HOST=user@your-gpu-machine
